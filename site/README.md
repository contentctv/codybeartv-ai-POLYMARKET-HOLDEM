# CIPHER COAST 3-IN-1 — Grok Build landing

One-page public door for the paper table. Canonical source repo:

https://github.com/contentctv/codybeartv-ai-POLYMARKET-HOLDEM

This folder is the Pages-ready paper-host copy. The night-table Vite app stays in the repo root (`npm run dev` on `:8080`). Do not replace that app with this static page.

A GitHub Pages copy also lives in `docs/`.

## Operator

1. Create a Stripe Payment Link $47 in **test mode**. Then type `STRIPE_LINK $47 https://buy.stripe.com/...` so a human can paste it into the Buy kit button. This build leaves `href="#"`.
2. Enable GitHub Pages on `site/` (or `docs/`) on this repo. A bot does not enable Pages.
3. Supercool Add Product fields live in `SUPERCOOL_SHOP_LISTING.md`.
4. Discord stays HOLD until a named lift.
5. WINGMAN does not push.

## Flags

- `STRIPE_LIVE=off`
- `DNS_CUTOVER=off`
- Discord HOLD
- Wallets off
- `LIVE_TRADING=false`
- GENERATE off
- grok-4.6 only — never invent grok-4.7
- Tip jar (Bitcoin Jungle, Fountain) is propina, not access

## Financial Engineering Notice

Education and a paper table. Not an offer of securities. Access is a kit or tuition. `cca_` is a login credential. LIVE_TRADING=false. Bitcoin Jungle is propina, not a purchase. CODYBEARTV GLOBAL MEDIA STUDIOS LIMITADA
